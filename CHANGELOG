v1.2.0. Extend the Kernel module with the retryable method so you can use it anywhere without having to include it in every class. Update gemspec, Gemfile, and Raketask.

v1.1.0. Setting default retry sleep period to random.

v1.0.0. First release.
